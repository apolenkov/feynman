Use **spaces** — 4 spaces per indent level.

This is the official recommendation from PEP 8, Python's style guide, and it's what virtually every major Python codebase, framework, and tool defaults to. Editors handle it transparently (pressing Tab inserts 4 spaces), and you'll avoid `TabError` issues that arise when tabs and spaces get mixed in the same file (Python 3 forbids mixing them).
