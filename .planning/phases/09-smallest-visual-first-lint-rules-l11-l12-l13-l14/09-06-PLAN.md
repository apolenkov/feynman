---
phase: 09-smallest-visual-first-lint-rules-l11-l12-l13-l14
plan: 06
type: execute
wave: 5
depends_on:
  - 09-01
  - 09-02
  - 09-03
  - 09-04
  - 09-05
files_modified:
  - docs/lint-rules.md
  - README.md
autonomous: true
requirements:
  - DOCS-L11
tags:
  - docs
must_haves:
  truths:
    - "docs/lint-rules.md documents L11, L12, L13 with valid + invalid examples and source line cross-references"
    - "docs/lint-rules.md intro updated from '10 rules (L01-L10)' to '13 rules (L01-L13)'"
    - "docs/lint-rules.md --fix section mentions L11 dot-leader autofix"
    - "README.md mentions --explain flag alongside --fix"
    - "Each new rule entry has a token-cost row showing chars saved"
  artifacts:
    - path: "docs/lint-rules.md"
      provides: "L11/L12/L13 reference"
      contains: "L11: Overdecoration"
    - path: "README.md"
      provides: "--explain mention"
      contains: "--explain"
  key_links:
    - from: "docs/lint-rules.md"
      to: "lib/lint/rules.js"
      via: "Source line cross-references"
      pattern: "lib/lint/rules.js"
---

<objective>
Document L11/L12/L13 in `docs/lint-rules.md`, update intro count to 13 rules, document `--fix` extension to L11 dot-leader, mention `--explain` flag in `README.md`.

Purpose: Closes DOCS-L11. Without docs, users encountering an L11 warn cannot understand WHY frame is wrong or HOW to fix.
Output: 3 new rule entries in docs/lint-rules.md following the L01-L10 template (What/Why/Source/Valid/Invalid). README.md updated with --explain mention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Inline foreground execution.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-smallest-visual-first-lint-rules-l11-l12-l13-l14/09-CONTEXT.md
@.planning/phases/09-smallest-visual-first-lint-rules-l11-l12-l13-l14/09-01-SUMMARY.md
@.planning/phases/09-smallest-visual-first-lint-rules-l11-l12-l13-l14/09-02-SUMMARY.md
@.planning/phases/09-smallest-visual-first-lint-rules-l11-l12-l13-l14/09-03-SUMMARY.md
@.planning/phases/09-smallest-visual-first-lint-rules-l11-l12-l13-l14/09-04-SUMMARY.md
@.planning/phases/09-smallest-visual-first-lint-rules-l11-l12-l13-l14/09-05-SUMMARY.md
@CLAUDE.md
@docs/lint-rules.md
@README.md

<interfaces>
Existing format in docs/lint-rules.md (each rule):
```markdown
## L0X: Name (severity: error|warn)

**What:** ...
**Why:** ...
**Source:** [`lib/lint/rules.js#LXX`](../lib/lint/rules.js)

### Valid
\`\`\`
... example ...
\`\`\`

### Invalid
\`\`\`text
... example ...
\`\`\`

Output:
\`\`\`text
file:N:1: L0X severity message
\`\`\`

---
```

The token-cost row uses the same shootout format as the research note:
| form | ~chars |
| frame block | ~280 |
| dot-leader | ~120 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add L11/L12/L13 entries + update intro in docs/lint-rules.md</name>
  <files>docs/lint-rules.md</files>
  <read_first>
    - docs/lint-rules.md (full file — use existing L01-L10 entries as templates)
    - lib/lint/rules.js (find the line numbers of L11_overdecoration, L12_token_budget, L13_double_wrap functions for source cross-references)
    - .planning/notes/token-economical-ascii-research-2026-05-10.md (token-cost shootout table for the rule entries)
  </read_first>
  <action>
    Step 1 — Update intro lines (lines 1-12 in docs/lint-rules.md). Replace:

    ```
    feynman includes a linter for ASCII diagrams in markdown files.
    Ten rules (L01-L10) enforce structural correctness.
    ```

    With:

    ```
    feynman includes a linter for ASCII diagrams in markdown files.
    Thirteen rules (L01-L13) enforce structural correctness and token economy.

    Rules L01-L09 enforce structural correctness (boxes close, trees end with └──, arrows are consistent, etc.). Rule L10 catches Cyrillic/Latin script mixing. Rules L11-L13 enforce token economy: don't use a frame for what a dot-leader can express; don't pad more than you fill; don't wrap a tree that already groups itself.
    ```

    Update the `--fix` description (currently mentions L08/L09 only):

    ```
    Add `--fix` to repair misaligned frame borders OR convert overdecorated frames to dot-leader:
    `feynman lint --fix <file.md>` — see [L08](#l08-frame-width-discipline-severity-error), [L09](#l09-right-edge-alignment-severity-error), and [L11](#l11-overdecoration-severity-warn).

    Add `--explain` to annotate each frame with token-cost breakdown:
    `feynman lint --explain <file.md>` — emits `framing: ~N chars; equivalent dot-leader: ~M chars; saving: -K`.
    ```

    Step 2 — Find the actual line numbers via:

    ```bash
    grep -n "^function L11_overdecoration" lib/lint/rules.js
    grep -n "^function L12_token_budget" lib/lint/rules.js
    grep -n "^function L13_double_wrap" lib/lint/rules.js
    ```

    Use those numbers in the Source links below (replace `LINE` placeholders).

    Step 3 — Append L11/L12/L13 entries AFTER the L10 entry (which is the last existing entry; locate end of file):

    ```markdown
    ---

    ## L11: Overdecoration (severity: warn)

    **What:** A frame block (`┌─...─┐ ... └─...─┘`) used to wrap ≤5 inner content lines. The frame's borders and padding waste tokens that a dot-leader list would not.

    **Why:** The trigger-table prescribes dot-leader for ≤5 status items. A 5-row frame at width 50 burns ~280 chars on borders + padding; the same data as a dot-leader list costs ~120. Compounded across many status replies per session, this is the largest single token-economy lever feynman has.

    **Whitelist:** frames containing nested trees (`├──` / `└──`) or embedded table columns (`│ key │ value │`) — those need explicit grouping that bare dot-leader cannot give.

    **Source:** [`lib/lint/rules.js#LINE`](../lib/lint/rules.js)

    ### Valid (same data as dot-leader)

    ```
    alpha ............ ok
    beta  ............ wait
    gamma ............ wip
    delta ............ done
    epsilon .......... done
    ```

    ### Invalid (frame for ≤5 items)

    ```text
    ┌──────────────────┐
    │ alpha .... ok    │
    │ beta  .... wait  │
    │ gamma .... wip   │
    │ delta .... done  │
    │ epsilon .. done  │
    └──────────────────┘
    ```

    Output:
    ```text
    file:1:1: L11 warn frame used for 5 items; consider dot-leader list (saves ~150 chars)
    ```

    Token-cost comparison:

    | form | ~chars | when uses |
    |------|--------|-----------|
    | inline pipe-separated | ~50 | context already set |
    | dot-leader list | ~120 | default for ≤5 items |
    | frame block | ~280 | ≥6 items, group needed |

    **Autofix:** `feynman lint --fix <file>` converts qualifying frames to dot-leader. Idempotent — running `--fix` twice produces zero further diff. Unicode markers (`✓ done`, `✗ fail`, `← готов`) and indentation are preserved.

    ---

    ## L12: Token Budget (severity: warn)

    **What:** A frame whose padding chars exceed its content chars. Padding includes outer `│` chars and trailing spaces inside each row; content is the trimmed inner text.

    **Why:** When a frame is mostly air, the cost is mostly air. Either tighten the frame to fit content or use a lighter visual.

    **Source:** [`lib/lint/rules.js#LINE`](../lib/lint/rules.js)

    ### Valid (content-dominated frame)

    ```
    ┌──────────────────────────────────┐
    │ deploy production rollout step a │
    │ deploy production rollout step b │
    │ deploy production rollout step c │
    └──────────────────────────────────┘
    ```

    ### Invalid (padding-dominated frame)

    ```text
    ┌────────────────────────────────────────┐
    │ ok                                     │
    │ no                                     │
    │ ok                                     │
    └────────────────────────────────────────┘
    ```

    Output:
    ```text
    file:1:1: L12 warn frame is padding-dominated (padding=120 > content=6); consider lighter visual
    ```

    **--explain flag:** `feynman lint --explain <file>` emits per-frame breakdown:

    ```text
    file:1: explain: framing block: ~280 chars (border: 12, padding: 168, content: 100)
    file:1: explain: equivalent dot-leader: ~120 chars
    file:1: explain: saving: -160 chars
    ```

    ---

    ## L13: Double Wrap (severity: warn)

    **What:** A tree (`├──` / `└──`) appearing inside a frame block. Tree indentation already conveys hierarchy — wrapping it adds zero information at full border cost.

    **Why:** A tree IS a visual structure. Wrapping it is double-encoding. The frame contributes nothing the indentation does not already deliver.

    **Source:** [`lib/lint/rules.js#LINE`](../lib/lint/rules.js)

    ### Valid (bare tree, no frame)

    ```
    project
    ├── src
    │   ├── index.js
    │   └── utils.js
    └── package.json
    ```

    ### Invalid (tree wrapped in frame)

    ```text
    ┌──────────────────┐
    │ project          │
    │ ├── src          │
    │ │   ├── index.js │
    │ │   └── utils.js │
    │ └── package.json │
    └──────────────────┘
    ```

    Output:
    ```text
    file:1:1: L13 warn tree inside frame block — the tree already conveys hierarchy; drop the frame
    ```

    **Note:** L11 (overdecoration) whitelists tree-in-frame because nested trees can sometimes justify visual grouping. L13 takes the opposite stance: even with grouping, the cost-benefit is negative. The two rules are complementary — L11 silent on tree-in-frame, L13 fires.
    ```

    Replace each `LINE` placeholder with the actual line number from the grep step.
  </action>
  <verify>
    <automated>grep -c "^## L11: Overdecoration" docs/lint-rules.md && grep -c "^## L12: Token Budget" docs/lint-rules.md && grep -c "^## L13: Double Wrap" docs/lint-rules.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## L11: Overdecoration" docs/lint-rules.md` returns 1
    - `grep -c "^## L12: Token Budget" docs/lint-rules.md` returns 1
    - `grep -c "^## L13: Double Wrap" docs/lint-rules.md` returns 1
    - `grep -c "Thirteen rules" docs/lint-rules.md` returns 1
    - `grep -c "L01-L13" docs/lint-rules.md` returns ≥1
    - `grep -c "\-\-explain" docs/lint-rules.md` returns ≥2
    - `grep -c "lib/lint/rules.js#" docs/lint-rules.md` returns ≥13 (one source link per rule)
    - All three rule entries include valid + invalid examples
    - All three rule entries cross-reference real source line numbers (no `LINE` placeholders left)
    - <done>docs/lint-rules.md documents 13 rules with valid/invalid examples</done>
  </acceptance_criteria>
  <done>docs/lint-rules.md updated</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md to mention --explain alongside --fix; commit docs</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — locate the --fix mention)
  </read_first>
  <action>
    Step 1 — Find the `--fix` mention in README.md:

    ```bash
    grep -n "\-\-fix" README.md
    ```

    Step 2 — Add `--explain` documentation alongside (insert after the --fix paragraph). Use `Edit` tool to make a precise change. Suggested addition:

    ```markdown
    Add `--explain` to annotate each frame block with token-cost breakdown:

    ```bash
    feynman lint --explain <file.md>
    ```

    Output:
    ```
    file:N: explain: framing block: ~280 chars (border: 12, padding: 168, content: 100)
    file:N: explain: equivalent dot-leader: ~120 chars
    file:N: explain: saving: -160 chars
    ```

    Use `--explain` to understand why L11 (overdecoration) or L12 (token-budget) fired and how many chars a lighter visual would save.
    ```

    Also update any "10 rules" / "L01-L10" mentions in README.md to "13 rules" / "L01-L13".

    Step 3 — Run `npm test` one last time to confirm full suite still green (docs changes can't break tests but verify regardless).

    Step 4 — Commit:

    ```bash
    git add docs/lint-rules.md README.md
    git commit -m "docs(09-06): document L11/L12/L13 + --explain flag (DOCS-L11)"
    ```
  </action>
  <verify>
    <automated>grep -c "\-\-explain" README.md && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\-\-explain" README.md` returns ≥1
    - If README mentions "10 rules" or "L01-L10", it has been updated to "13 rules" / "L01-L13"
    - `npm test` exits 0
    - Last commit subject matches `docs(09-06): document L11/L12/L13 + --explain flag (DOCS-L11)`
    - <done>README + docs updated, full suite green, Phase 9 work complete</done>
  </acceptance_criteria>
  <done>Plan 09-06 complete; Phase 9 ready for milestone retro</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Docs-only changes; no runtime or trust impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Information Disclosure | Stale source-line cross-references in docs | accept | Line numbers refreshed via grep at task time; future drift is a separate doc-maintenance concern |
</threat_model>

<verification>
- All Phase 9 rules documented
- Intro count updated
- README mentions --explain
- `npm test` still green
</verification>

<success_criteria>
- L11/L12/L13 documented with What/Why/Source/Valid/Invalid format
- Intro updated from "10 rules" to "13 rules"
- --fix section mentions L11 dot-leader
- --explain documented in both docs/lint-rules.md and README.md
- All cross-references resolve to real lib/lint/rules.js line numbers
- 1 atomic commit
</success_criteria>

<output>
Create `.planning/phases/09-smallest-visual-first-lint-rules-l11-l12-l13-l14/09-06-SUMMARY.md` confirming all DOCS-L11 acceptance items covered.
</output>
