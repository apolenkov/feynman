---
phase: 08-prompt-architecture-rewrite-xml-contract-token-economy-suppr
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - PROMPT-12  # Acceptance criterion: README contains compaction-survivor explanation
tags: [docs, readme, compaction-survivor]
must_haves:
  truths:
    - "README.md contains a section or paragraph explaining why feynman uses UserPromptSubmit (not SessionStart)"
    - "The explanation references compaction survival in concrete terms (every turn vs once per session)"
  artifacts:
    - path: "README.md"
      provides: "compaction-survivor value-prop section (3-4 sentences per CONTEXT.md <specifics>)"
      contains: "compaction"
  key_links: []
---

<objective>
Add a 3-4 sentence section to `README.md` titled "Why feynman uses UserPromptSubmit (not SessionStart)" explaining the compaction-survivor value-prop: `UserPromptSubmit` re-injects rules on every turn so they survive Claude Code's automatic context compaction, whereas `SessionStart` fires only once and the rules are lost the moment context is compacted.

Purpose: closes the only remaining SPEC acceptance criterion not handled by Plan 02 (the README compaction-survivor explanation, criterion 12 in the SPEC acceptance list). Runs in parallel with Plan 01 (different file, no shared state).

Output: small README addition placed in the "Why feynman" area (or as a sibling subsection); no badges, no API reference, no install changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-SPEC.md
@.planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-CONTEXT.md
@README.md

<interfaces>
<!-- Section title (CONTEXT.md <specifics> line 176): -->
<!--   "Why feynman uses UserPromptSubmit (not SessionStart)" -->

<!-- Length budget: 3-4 sentences (CONTEXT.md <specifics>) -->

<!-- Substance: -->
<!--   - SessionStart fires once when Claude Code starts a session              -->
<!--   - Claude Code automatically compacts context as the conversation grows   -->
<!--   - After compaction, anything injected by SessionStart is lost            -->
<!--   - UserPromptSubmit re-runs the hook on every prompt, so rules are        -->
<!--     re-injected after every compaction event — they survive                -->

<!-- Placement: -->
<!--   The current README has a "Why feynman" section near the top (line ~50). -->
<!--   The new section should sit either inside that block or directly after   -->
<!--   it, BEFORE the "Install" section. Keep it scannable (one short heading +-->
<!--   a 3-4 sentence paragraph).                                              -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compaction-survivor section to README.md</name>
  <files>README.md</files>
  <behavior>
    Insert a new subsection (h3 or h4 heading, matching existing README hierarchy) titled "Why feynman uses UserPromptSubmit (not SessionStart)" containing 3-4 sentences explaining:
    1. SessionStart fires once at session start
    2. Claude Code compacts context as conversations grow long
    3. SessionStart-injected rules are lost on compaction
    4. UserPromptSubmit re-injects on every turn — rules survive compaction

    The section sits inside or immediately after the existing "Why feynman" section, BEFORE the "Install" section. The TOC at the top of README (line ~25-32) gets a new anchor entry pointing to the new heading slug.
  </behavior>
  <action>
    Locate the "Why feynman" section header (around line 50 of README.md) and insert the new subsection after the existing paragraph but before the next h2. Match the existing heading style (h2 `##` for top sections, h3 `###` for subsections — current README uses `##` for top-level sections).

    Suggested phrasing (factual, declarative — no marketing voice):

    > ### Why feynman uses UserPromptSubmit (not SessionStart)
    >
    > Claude Code compacts the context window automatically as a conversation grows. Anything injected by a `SessionStart` hook is part of that early context and is lost the moment compaction runs. The `UserPromptSubmit` hook fires on every turn, so feynman re-injects its rules after every compaction event — the diagram contract survives the entire session, not just the opening turn.

    Add a corresponding entry to the table-of-contents `<p align="center">` block at the top (e.g. `<a href="#why-feynman-uses-userpromptsubmit-not-sessionstart">Compaction</a>`).

    Keep the diff bounded — do not modify badges, install instructions, or other sections.
  </action>
  <verify>
    <automated>grep -ciE 'compaction|UserPromptSubmit' README.md | head -1 &amp;&amp; grep -c 'Why feynman uses UserPromptSubmit' README.md</automated>
  </verify>
  <done>
    `grep -c 'Why feynman uses UserPromptSubmit' README.md` ≥ 1. `grep -ciE 'compaction' README.md` ≥ 2 (heading + body). Section is 3-4 sentences. TOC anchor added (or skipped with explicit note in SUMMARY.md if README TOC structure does not support per-subsection anchors). No other README sections modified.
  </done>
</task>

</tasks>

<verification>
- `grep -c 'Why feynman uses UserPromptSubmit' README.md` ≥ 1
- `grep -c 'compaction' README.md` ≥ 1
- `git diff --stat README.md` shows ≤ 12 lines added (3-4 sentences + heading + TOC entry)
- All other files unchanged
</verification>

<success_criteria>
Closes the README compaction-survivor acceptance criterion in the SPEC acceptance list (criterion 12 of 14). Independent of Plan 01 and Plan 02 — this plan can run in Wave 1 in parallel with the parser migration.
</success_criteria>

<output>
After completion, create `.planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-03-SUMMARY.md` with the final paragraph wording chosen, line where it was inserted, and commit SHA.
</output>
