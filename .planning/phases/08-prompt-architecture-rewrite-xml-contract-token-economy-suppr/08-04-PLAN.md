---
phase: 08-prompt-architecture-rewrite-xml-contract-token-economy-suppr
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - feynman-rules-workspace/iteration-2/
  - .planning/notes/eval-iteration-2-findings-2026-05-09.md
  - .planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-VERIFICATION.md
autonomous: false
requirements:
  - PROMPT-02  # SPEC §2 — eval-13/15 negative-case re-run (zero frames AND zero MD tables AND zero priority markers)
  - PROMPT-04  # SPEC §4 — eval-07/08 ≤1 primary visual per response
  - PROMPT-09  # SPEC §9 — eval-20/17 ≤1 MD table AND zero frames AND ≤60% baseline bytes
  - PROMPT-A11 # SPEC acceptance §A.C-11 — full iteration-2 A/B re-run results
tags: [eval, ab-test, iteration-2, verification]
must_haves:
  truths:
    - "feynman-rules-workspace/iteration-2/ contains baseline + with_old_rules + with_new_rules outputs for all 20 evals (3-way A/B per CONTEXT.md Area H)"
    - "eval-13 (single-fact) and eval-15 (prose-opt-out) under with_new_rules emit zero ASCII frames AND zero MD tables AND zero priority markers (SPEC §2 acceptance)"
    - "eval-20 (definition) and eval-17 (recommendation) under with_new_rules emit ≤1 MD table AND zero frames AND total bytes ≤60% of baseline (SPEC §9 acceptance)"
    - "eval-07 (status) and eval-08 (priority) under with_new_rules emit ≤1 primary visual per response (SPEC §4 acceptance)"
    - "eval-05 (comparison-3-databases) under with_new_rules uses MD table — not ASCII pipes inside code block"
    - "eval-01 (sequence) and eval-10 (branching) under with_new_rules contain at least one ASCII flow diagram (WIN-class preservation)"
    - "All 14 SPEC acceptance criteria are checked off in 08-VERIFICATION.md with evidence (command output, file path, byte count, visual count)"
  artifacts:
    - path: "feynman-rules-workspace/iteration-2/"
      provides: "20 eval directories × 3 arms (baseline, with_old_rules, with_new_rules) = 60 answer.md files"
    - path: ".planning/notes/eval-iteration-2-findings-2026-05-09.md"
      provides: "quantitative summary table (bytes per arm) + visual-count table + WIN/HURT/NEUTRAL classification per eval"
    - path: ".planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-VERIFICATION.md"
      provides: "checkbox list of all 14 SPEC acceptance criteria with pass/fail evidence per criterion"
  key_links:
    - from: ".planning/notes/eval-iteration-2-findings-2026-05-09.md"
      to: "feynman-rules-workspace/iteration-2/"
      via: "byte/visual counts derived from answer.md files"
      pattern: "iteration-2/eval-"
---

<objective>
Run the iteration-2 A/B harness on the rewritten rule file and produce a verification document that ticks off all 14 SPEC acceptance criteria with command-output evidence.

Per CONTEXT.md Area H: reuse the iteration-1 subagent dispatcher verbatim (same `general-purpose` subagent, same prompt template, same `evals/evals.json`), but add a third arm `with_old_rules` so we get a true 3-way A/B (baseline / old rules / new rules). This answers two questions in one run: "did we improve over the existing rules?" and "did we change behavior at all relative to baseline?"

The plan also produces `08-VERIFICATION.md` — the single document a reviewer reads to confirm Phase 8 is done. Each of the 14 SPEC acceptance criteria gets one row with the exact command run, the captured output, and a pass/fail flag.

Purpose: closes SPEC acceptance criteria 11 (iteration-2 A/B) and consolidates evidence for criteria 1-10, 12-14 already established by Plans 01-03. Without this consolidation, the phase has no single artifact saying "all 14 boxes ticked".

Output: `feynman-rules-workspace/iteration-2/eval-NN/{baseline,with_old_rules,with_new_rules}/outputs/answer.md` for all 20 evals (60 files); `eval-iteration-2-findings-2026-05-09.md` with the quantitative summary; `08-VERIFICATION.md` with the 14-criterion checkbox list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-SPEC.md
@.planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-CONTEXT.md
@.planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-01-SUMMARY.md
@.planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-02-SUMMARY.md
@.planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-03-SUMMARY.md
@.planning/notes/eval-iteration-1-findings-2026-05-09.md
@evals/evals.json
@rules/feynman-activate.md

<interfaces>
<!-- Iteration-1 directory layout (CONTEXT.md Area H — replicate exactly): -->
<!--   feynman-rules-workspace/iteration-1/eval-NN-<slug>/{baseline,with_rules}/outputs/answer.md -->

<!-- Iteration-2 directory layout (add third arm): -->
<!--   feynman-rules-workspace/iteration-2/eval-NN-<slug>/{baseline,with_old_rules,with_new_rules}/outputs/answer.md -->

<!-- Subagent prompt templates (CONTEXT.md Area H, eval-iteration-1-findings line 144-147): -->
<!--   baseline:        "Answer naturally. No rules/skills. PROMPT: <eval prompt>. Write to <path>." -->
<!--   with_old_rules:  "Read /tmp/feynman-rules-old-full.md. Apply ALL 'full' intensity rules. PROMPT: <eval prompt>. Write to <path>." -->
<!--   with_new_rules:  "Read /tmp/feynman-rules-new-full.md. Apply ALL 'full' intensity rules. PROMPT: <eval prompt>. Write to <path>." -->

<!-- The "old" full intensity is captured from the v0.2.7 rule file BEFORE Plan 02 commit:        -->
<!--   git show v0.2.7:rules/feynman-activate.md | awk '/<!-- full -->/,/<!-- \/full -->/' > /tmp/feynman-rules-old-full.md -->
<!-- The "new" full intensity is extracted from the post-Plan-02 file via the Plan 01 hook regex: -->
<!--   node -e 'const fs=require("fs");const r=fs.readFileSync("rules/feynman-activate.md","utf8"); const m=/<intensity\s+name=["\x27]full["\x27]\s*>([\s\S]*?)<\/intensity>/.exec(r); fs.writeFileSync("/tmp/feynman-rules-new-full.md", m[1].trim());' -->

<!-- 14 SPEC acceptance criteria (08-SPEC.md lines 113-132) — verification target: -->
<!--   1.  rules file contains <triggers> <syntax> <examples> <contract> at least once each -->
<!--   2.  rules file contains exactly three <intensity name="lite|full|ultra"> blocks       -->
<!--   3.  rules file contains zero <!-- lite --> / <!-- full --> / <!-- ultra --> markers   -->
<!--   4.  wc -c rules/feynman-activate.md ≤ 4480                                            -->
<!--   5.  each intensity block has structure→visual decision table with ≥7 rows             -->
<!--   6.  <contract> contains words classify channel amplify suppress                       -->
<!--   7.  SDLC pattern section contains words mutex OR select ONE                           -->
<!--   8.  suppression guidance names definition / recommendation / question-back / greeting -->
<!--   9.  npm test exits 0 with ≥227 passing tests                                          -->
<!--  10.  ≥1 new test in tests/hook.test.js asserts XML-tag extraction                      -->
<!--  11.  iteration-2 A/B re-run satisfies all six sub-criteria (eval-13/15, 20/17, 7/8, 5, 1/10) -->
<!--  12.  README contains compaction-survivor section                                       -->
<!--  13.  Migration commits isolated: parser commit + rule-content commit (separate SHAs)   -->
<!--  14.  Q-2026-05-09-01 has Status: answered (already true — see findings file)           -->

<!-- Visual counters (must be reproducible by grep, not by eyeball): -->
<!--   ASCII frame:    grep -cE '^[+┌].*[+┐]$' answer.md                                     -->
<!--   MD table:       grep -cE '^\|.*\|$' answer.md (count contiguous rows / 2 for distinct tables) -->
<!--   priority scale: grep -cE '^[▲▼]' answer.md                                            -->
<!--   arrow flow:     grep -cE '\[.*\][[:space:]]*(-->|→)' answer.md                        -->
<!--   ASCII tree:     grep -cE '├──|└──' answer.md                                          -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run iteration-2 3-way A/B harness on all 20 evals</name>
  <files>feynman-rules-workspace/iteration-2/, .planning/notes/eval-iteration-2-findings-2026-05-09.md</files>
  <action>
    Step 1 — capture old + new full-intensity rule snapshots:
    ```bash
    git show HEAD~2:rules/feynman-activate.md | awk '/<!-- full -->/{flag=1;next}/<!-- \/full -->/{flag=0}flag' > /tmp/feynman-rules-old-full.md
    node -e 'const fs=require("fs");const r=fs.readFileSync("rules/feynman-activate.md","utf8");const m=/<intensity\s+name=["\x27]full["\x27]\s*>([\s\S]*?)<\/intensity>/.exec(r);if(!m){throw new Error("no full intensity block found");}fs.writeFileSync("/tmp/feynman-rules-new-full.md", m[1].trim());'
    ```
    (HEAD~2 assumes Plan 01 commit + Plan 02 commit landed in that order; if commit graph differs, locate the pre-Plan-02 SHA via `git log --oneline rules/feynman-activate.md` and substitute.)

    Step 2 — for each of the 20 evals in `evals/evals.json`, dispatch a `general-purpose` subagent for each of the three arms. Use the EXACT prompt templates from CONTEXT.md Area H (see <interfaces> above). Output paths:
    ```
    feynman-rules-workspace/iteration-2/eval-NN-<slug>/baseline/outputs/answer.md
    feynman-rules-workspace/iteration-2/eval-NN-<slug>/with_old_rules/outputs/answer.md
    feynman-rules-workspace/iteration-2/eval-NN-<slug>/with_new_rules/outputs/answer.md
    ```
    Total: 60 answer.md files. Reuse the iteration-1 slug naming (eval-01-sequence-deploy, etc.) — the existing iteration-1 directory provides the canonical slugs.

    Step 3 — produce `.planning/notes/eval-iteration-2-findings-2026-05-09.md` with:
    - quantitative summary: bytes per arm per eval (3-column table, 20 rows)
    - visual count table: ASCII frames / ASCII trees / arrow flows / MD tables / priority markers per arm per eval
    - WIN / HURT / NEUTRAL classification vs old rules (delta column)
    - explicit verdict on each iteration-1 failure mode: did rewrite fix comparison-05 (MD table not pipes), status-07 (≤1 visual), priority-08 (≤1 visual), definition-20 (suppression effective)?

    Mirror the structure of `.planning/notes/eval-iteration-1-findings-2026-05-09.md` so a reader who knows iteration-1 can read iteration-2 in <5 minutes.

    Cost note: this is the heaviest task in the phase. ~60 subagent dispatches. Plan for ~30-40% context budget on this single task; if the executor sees context climbing past 50%, dispatch in batches of 5 evals (15 subagents per batch) and write findings table incrementally.
  </action>
  <verify>
    <automated>find feynman-rules-workspace/iteration-2 -name 'answer.md' | wc -l | awk '{exit ($1 &gt;= 60) ? 0 : 1}' &amp;&amp; test -f .planning/notes/eval-iteration-2-findings-2026-05-09.md</automated>
  </verify>
  <done>
    60 answer.md files exist (20 evals × 3 arms). `eval-iteration-2-findings-2026-05-09.md` contains: quantitative bytes table, visual count table, WIN/HURT/NEUTRAL classification, explicit verdict on each iteration-1 failure mode (comparison-05, status-07, priority-08, definition-20). The findings file is ≤200 lines (mirrors iteration-1 length).
  </done>
</task>

<task type="auto">
  <name>Task 2: Produce 08-VERIFICATION.md with all 14 SPEC criteria checked</name>
  <files>.planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-VERIFICATION.md</files>
  <action>
    Create `08-VERIFICATION.md` as a single document where each row corresponds to one of the 14 SPEC acceptance criteria. Each row contains:
    - criterion text (verbatim from SPEC §"Acceptance Criteria")
    - evidence command (the exact bash invocation used to verify)
    - captured output (truncated to ≤3 lines per row)
    - pass/fail flag (✓ / ✗)
    - link to the supporting plan SUMMARY (08-01-SUMMARY.md / 08-02-SUMMARY.md / 08-03-SUMMARY.md / this plan)

    Layout (markdown table inside frame block, since this is a status table per CLAUDE.md visuals rule):

    ```markdown
    ## Phase 8 Acceptance Verification

    | # | Criterion | Evidence command | Result | ✓/✗ |
    |---|-----------|------------------|--------|-----|
    | 1 | rules file contains <triggers>… | `grep -c '<triggers>' rules/feynman-activate.md` | 3 | ✓ |
    | … | … | … | … | … |
    ```

    For criterion 11 (the iteration-2 A/B compound criterion), expand into 6 sub-rows (one per sub-criterion: eval-13/15, eval-20/17, eval-07/08, eval-05, eval-01/10) and show the visual-count grep output for each.

    For criterion 13 (isolated migration commits), include `git log --oneline --grep='hook' --grep='rules' rules/ hooks/` output showing the two distinct SHAs.

    If any criterion fails: do NOT silently mark it ✗ and move on. Diagnose the gap, fix it (re-edit the relevant artifact), re-run verification, and only then mark ✓. The point of this document is "Phase 8 is actually done" — partial completion is not acceptable.
  </action>
  <verify>
    <automated>test $(grep -cE '^\| ([0-9]+|[0-9]+\.[a-z]) \|' .planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-VERIFICATION.md) -ge 14 &amp;&amp; ! grep -E '✗' .planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-VERIFICATION.md</automated>
  </verify>
  <done>
    `08-VERIFICATION.md` exists. Contains ≥ 14 numbered criterion rows (or 19 with the 6 sub-rows of criterion 11). Every row marked ✓ — zero ✗ marks. Every evidence cell contains real command output (not "TBD" or "N/A"). The document doubles as the phase's exit gate: a reviewer can read it top-to-bottom and confirm Phase 8 ships.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of iteration-2 deltas before declaring Phase 8 shipped</name>
  <what-built>
    - Plan 01: dual-format hook parser + XML extraction tests
    - Plan 02: rewritten rules/feynman-activate.md as XML contract (≤4480 bytes), CLAUDE.md updated
    - Plan 03: README compaction-survivor section
    - Plan 04 Task 1: 60 iteration-2 answer.md files + eval-iteration-2-findings-2026-05-09.md
    - Plan 04 Task 2: 08-VERIFICATION.md with all 14 SPEC criteria marked ✓
  </what-built>
  <how-to-verify>
    The user (developer) reads `eval-iteration-2-findings-2026-05-09.md` and `08-VERIFICATION.md` and confirms three things mechanical grep cannot:

    1. Open `feynman-rules-workspace/iteration-2/eval-20-definition/with_new_rules/outputs/answer.md` and confirm the suppression rule produced a prose answer (not 9 MD tables like iteration-1 baseline). Compare against `feynman-rules-workspace/iteration-2/eval-20-definition/baseline/outputs/answer.md`.

    2. Open `feynman-rules-workspace/iteration-2/eval-05-comparison/with_new_rules/outputs/answer.md` and confirm comparison renders as a Markdown table — NOT as ASCII pipes inside a code block (this was the iteration-1 HURT failure mode).

    3. Open `feynman-rules-workspace/iteration-2/eval-15-prose/with_new_rules/outputs/answer.md` and confirm the model produced prose with zero frames AND zero MD tables AND zero priority markers (negative-case preservation — this was the F2-cancelled rationale).

    These three reads catch failure modes that grep counters might miss (e.g., the model emits the right shape but wrong content; the model uses Unicode lookalikes that escape the grep regex).

    Resume by typing "approved" if all three look correct. If any read shows a regression, type "regress in eval-NN" and the executor will spawn a focused fix (likely a Plan 02 follow-up to tighten the relevant trigger-table row or suppression list phrasing).
  </how-to-verify>
  <resume-signal>Type "approved" or "regress in eval-NN: <description>"</resume-signal>
  <files>(checkpoint — no files written by this task; reads outputs from Task 1)</files>
  <action>Pause execution and surface the &lt;how-to-verify&gt; instructions to the user. Do not proceed to phase-completion or commit until the resume-signal is received. If the user replies "approved", continue. If the user replies "regress in eval-NN", spawn a focused fix targeting the trigger-table row or suppression-list phrasing implicated by that eval, then re-run only the affected eval-NN through the iteration-2 harness, update findings + verification docs, and re-prompt for review.</action>
  <verify>
    <automated>MISSING — checkpoint task; verification is human inspection of three answer.md files per &lt;how-to-verify&gt;</automated>
  </verify>
  <done>User has typed "approved" in chat OR every "regress in eval-NN" report has been resolved with a follow-up commit and re-verification.</done>
</task>

</tasks>

<verification>
- `find feynman-rules-workspace/iteration-2 -name 'answer.md' | wc -l` ≥ 60
- `.planning/notes/eval-iteration-2-findings-2026-05-09.md` exists, ≥80 lines
- `.planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-VERIFICATION.md` exists with all 14 criteria marked ✓
- `git log --oneline -- rules/feynman-activate.md hooks/feynman-activate.js | head -3` shows ≥2 distinct SHAs (parser commit + rule-content commit, isolated per CONTEXT.md Area G)
- `grep -A 1 'Status:' .planning/research/Q-2026-05-09-01-findings.md | grep -c answered` ≥ 1 (already-satisfied baseline check; criterion 14)
</verification>

<success_criteria>
Phase 8 is verifiable as done. The single artifact a reviewer reads is `08-VERIFICATION.md` — every SPEC acceptance criterion has command-output evidence and a ✓ flag. Iteration-2 evidence file establishes that the rewrite produced the intended amplify/channel/suppress behavior on the same 20-eval set used for iteration-1 (apples-to-apples per CONTEXT.md Area H constraint).

If the human-verify checkpoint surfaces a regression, the recovery path is a focused Plan 02 follow-up — NOT a re-plan. The migration commit isolation (CONTEXT.md Area G) means the rule-content commit can be reverted without touching the hook parser or README.
</success_criteria>

<output>
After completion, create `.planning/phases/08-prompt-architecture-rewrite-xml-contract-token-economy-suppr/08-04-SUMMARY.md`:
- iteration-2 quantitative deltas summary (3 numbers: avg bytes baseline / old-rules / new-rules across 20 evals)
- WIN / HURT / NEUTRAL counts vs old rules
- list of any criteria that needed rework before going ✓ in 08-VERIFICATION.md (provenance)
- final commit SHA chain for the phase (parser → rules → README → iteration-2 evidence)
</output>
