# Agent Instructions

This repository uses `CLAUDE.md` as the canonical project memory generated by
GSD. Codex and other AGENTS.md-aware tools should follow the same project
contract instead of maintaining a divergent copy.

Read and follow:

@CLAUDE.md

## Codex Notes

- Treat Claude Code and Codex as first-class supported targets.
- Preserve the zero-runtime-dependency, CommonJS-only package contract.
- Before changing files, use the GSD workflow context in `.planning/`.
- Keep `AGENTS.md` as this small compatibility shim; update `CLAUDE.md` and
  GSD source artifacts when project memory changes.
